import { brand as designBrand } from '../../../packages/design/src/brand';
import type { Brand as DesignBrand } from '../../../packages/design/src/brand';

export const brand: DesignBrand = designBrand;